import { PostProcessPipe } from './post-process.pipe';

describe('PostProcessPipe', () => {
  it('create an instance', () => {
    const pipe = new PostProcessPipe();
    expect(pipe).toBeTruthy();
  });
});
